git push origin main
git log --oneline --graph --decorate --all
git branch
git checkout -b first-branch
git remote -v

fetch vs pull

```
$ git remote -v (see remotes: fork (my repo) vs origin (your repo))
origin  https://github.com/epeterssen/testing.git (fetch)
origin  https://github.com/epeterssen/testing.git (push)
```